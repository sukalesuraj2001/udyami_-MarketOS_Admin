export const environment = {
  production: false,
  // apiBaseUrl: 'https://backend.udyamikutumba.com',
  apiBaseUrl: ' http://localhost:3000',
  /**
   * When true, the HTTP layer is served by MockApiInterceptor instead of a
   * real backend. Flip to false once the NestJS API is available — no
   * component or service code needs to change.
   */
  useMockApi: false,
};
